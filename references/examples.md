# Examples

Formatting files in a disposable branch with a verifier is `AUTO`. A production deployment with a known rollback is `GUARD`. Brand direction is `COCREATE`. A privacy-impacting architecture decision is `CHALLENGE`. Writing a personal apology, where authenticity is the point, is `HUMAN_ONLY`.
